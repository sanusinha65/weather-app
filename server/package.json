{
  "name": "weather-app-server",
  "version": "1.0.0",
  "private": true,
  "description": "Backend proxy + caching layer for the Weather App (keeps the OpenWeather API key off the client).",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "axios": "^1.7.9",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2"
  }
}
